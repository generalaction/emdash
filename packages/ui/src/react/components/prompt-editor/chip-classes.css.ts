/**
 * Generated class assigned directly by the TipTap mention and slash-command
 * extensions. The class is preserved in serialized ProseMirror HTML without
 * requiring a document-global semantic selector.
 */
import { tokens } from '@emdash/theme';
import { style } from '@styles/index';

export const serializedEditorChip = style({
  display: 'inline',
  fontSize: '0.75rem',
  fontWeight: 400,
  borderRadius: '0.125rem',
  paddingLeft: '0.25rem',
  paddingRight: '0.25rem',
  paddingTop: '0.125rem',
  paddingBottom: '0.125rem',
  backgroundColor: tokens.surface.current.hover,
  color: tokens.foreground.default,
  boxShadow: '0 0 0 1px rgba(0,0,0,0.1)',
  verticalAlign: 'baseline',
});
