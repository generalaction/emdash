/**
 * Duration of the height transition. Shared between the CSS transition in
 * animated-height.css.ts and the component's settle-timer fallback so the
 * two can never drift apart.
 */
export const HEIGHT_TRANSITION_DURATION_MS = 250;
